# Public browser-safe values from Supabase project settings.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
VITE_MAP_STYLE_URL=https://tiles.openfreemap.org/styles/liberty

# Keep false in Vercel. Set true only for intentional local prototype work.
VITE_ENABLE_DEMO_MODE=false

# Server-only analytics ingestion. Never prefix these values with VITE_.
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Never add SUPABASE_SERVICE_ROLE_KEY to Vite or client-side source files.

# Server-only email delivery values. Never prefix these with VITE_.
RESEND_API_KEY=
CONVERSATION_TO_EMAIL=
CONVERSATION_FROM_EMAIL=Puente ATX <onboarding@resend.dev>
